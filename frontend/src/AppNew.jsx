/**
 * AppNew.jsx was an exact copy of App.jsx. It now re-exports the same
 * component so the two entry points can never drift out of sync —
 * importing from either path gives you the identical app.
 */
export { default } from './App.jsx';
